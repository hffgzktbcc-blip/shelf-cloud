# Copy this file to /mnt/onboard/.adds/nm/ on the Kobo.
# Put your token in shelf-sync.conf first.
#
# The 45000 is how long NickelMenu waits for output. Finding Shelf is usually instant,
# but a first run that has to sweep the network needs more than the default 9 seconds.
menu_item:main:Shelf sync:cmd_output:45000:/mnt/onboard/.adds/shelf-sync/shelf-sync.sh
